# 答辩 · CORS 与缓存 10 问

> 用法:整个文件发给 AI,逐题作答,它逐题追问。通过线 **≥8**。

1. 同源策略是谁执法的?为什么 curl/Postman 永远测不出 CORS 问题?这个事实如何改变你排查 CORS 的方法?
2. 预检请求的完整报文长相(方法 + 两个关键请求头 + 服务端该回什么)?预检失败时,真正的业务请求发出去了吗?
3. 哪些请求免预检?"表单三兄弟"白名单为什么存在?用"策略只能增量演进"的语言讲。
4. Cookie(HttpOnly) 与 localStorage(JWT) 各免疫什么攻击、害怕什么攻击?给出选型及理由。
5. fetch 的 Promise 什么时候 reject?"网络通但 404"为什么不 reject?这个设计让你必须写哪行代码?
6. `res.json()` 为什么不能调用两次?`Response.body` 的形态(流/值)还带来了什么特性(如背压/内存)?
7. queryKey 与 Caffeine 的 key 有什么本质相同点?queryKey 里少放一个变量会发生什么?用"缓存串数据"的事故语言讲。
8. staleTime 与 gcTime 各管什么?(提示:一个管"新鲜度",一个管"回收"——Caffeine 里各对应什么?)
9. 支付成功后为什么要 invalidate 而不是把新数据写进缓存?"读永远回源"换来的是什么保证?
10. **综合题**:给团队写一条"前端接后端接口"的规范,至少覆盖:res.ok 检查、超时、queryKey 设计、写后失效——每条附机制层理由。

---

*通过后:错题进误解本 → 修补 → AI 出变体题复测。S30 考官会抽 3 道口试。*
