# 《全栈之旅》教程方案 v0.1

> 副标题：**写给 Java 后端工程师的前端通关手册**
> 备选名：《前端，不过是另一台你早就见过的计算机》《从 JVM 到 Fiber：一个后端的全栈之旅》
> 形式：参考 ai4me / llm-journey —— 一张旅程地图作主线，知识点切成"站点"，每站一篇短文，站与站之间有叙事衔接。

---

## 1. 一句话定位

不教"前端知识点"，教**"你已经会的东西如何在前端换了个名字"**。

读者是一位扎实的 Java 后端工程师：懂操作系统、网络、JVM、并发。市面上 99% 的前端教程假设读者是小白，从"什么是变量"讲起——这对你是巨大的浪费，也是巨大的无聊。本教程反着来：**每一站都从你熟悉的概念出发，抵达前端的新概念，并且明确标出"类比在哪里失效"**。

## 2. 读者画像与起点假设

- 熟练 Java / Spring / JVM，理解堆、栈、GC、类加载、线程池、锁、NIO。
- 懂 HTTP、TCP/IP、REST、缓存（Caffeine/Redis）、消息队列。
- **零前端经验**（或写过但一直似懂非懂）。
- 学习目标：能独立完成 TypeScript + React 的 Web 前端开发，并能用 React Native 做出可上架的简单 App；理解"为什么"而不只是"怎么写"。

## 3. 核心方法论

### 3.1 主旋律：四条不变量（回应你的核心洞察）

你的判断是全书的地基，原话提炼如下——**前后端都是数据读写，前端的数据读写用于交互与渲染，后端的用于计算；而背后的内存管理、进程管理、通信（共享、通道、锁）逻辑不变**。

把它升格为贯穿全书的主旋律：每一站都会回到同一张"不变量表"上打卡，读者每学一个新东西，就往表里填一格。全书结束时，这张表就是你的"全栈统一场理论"。

| 不变量 | Java 后端世界 | 浏览器前端世界 | 出现的站点 |
|---|---|---|---|
| **数据放在哪**（内存模型） | JVM 堆、栈帧、元空间、堆外内存 | JS 堆（闭包、Fiber 槽位）、渲染树、localStorage/IndexedDB（前端的"磁盘"） | S1、S2 |
| **谁说了算**（调度模型） | OS 线程调度、线程池、优先级队列 | 事件循环单线程调度、微/宏任务队列、浏览器多进程架构 | S0、S1、S2 |
| **怎么不打架**（同步与通信） | synchronized/Lock、CAS、并发容器、消息队列 | 不可变状态（≈Copy-on-Write）、单向数据流、事件订阅、Context | S2、S3 |
| **数据怎么流动**（I/O 模型） | BIO/NIO/Netty、数据库、多级缓存 | fetch/XHR、WebSocket/SSE、TanStack Query 本地缓存 | S3、S4 |

> 一句话版本：**前端不是另一个学科，是同一套"数据读写物理学"在另一种介质上的应用。**

### 3.2 单篇三层映射法（每篇文章的骨骼）

1. **对暗号**——开篇用读者已知的 OS/Java 概念搭脚手架："这个东西你认识，它叫 XXX，只是换了个工牌。"
2. **划边界**——专设一节"类比失效的地方"。类比是脚手架不是承重墙，凡是会误导的地方必须明说。这是与市面教程最大的差异点，也是深度所在。
3. **动手练**——每篇一个 5~10 分钟能跑通的最小实验（labs/ 目录，可运行），并附"预期翻车现场"：常见报错原文 + 翻译成人话。

### 3.3 ShowCase：你提出的"Hook 是内存管理"如何落地

这个洞察非常好，直接成为全书的招牌章节（第 06 站）。展示一下讲法：

> 你写 Java 时想过没有：`@Service` 的 Bean，凭什么第二次注入还是原来那个对象？因为它的状态活在 IoC 容器的堆里，而不是方法的栈帧里——方法执行完，栈帧就销毁了，但对象有户口。
>
> `useState` 干的是同一件事。React 函数组件每次渲染都是**从头到尾重新执行一遍的纯函数**，栈帧里的局部变量每次都是新的。那它凭什么记得住上一次的值？因为 useState 压根没把值放在栈帧里——它主动让状态**逃逸**到堆上，挂进组件在 Fiber 节点上的"户口本"（一条按调用顺序排列的 Hook 链表）。
>
> 所以"Rules of Hooks"（不能写在 if 里）根本不是语法规定，而是**调用约定**：Hook 按调用顺序在链表上寻址，你在 if 里少调一个，后面的 Hook 全部槽位错位——相当于寄存器分配表被扯歪了，不 segfault 已经很给面子了。

（逃逸分析、调用约定、寄存器槽位——全是计算机组成原理和 JVM 的老家话术。）

## 4. 旅程地图（六大站段 · 18 站）

```
⛺ 出发营地
   │
   ▼
🏭 S0 装备检查站（TS 与异步）──────── 00 01 02
   │
   ▼
⚙️ S1 运行时站（浏览器即操作系统）── 03 04
   │
   ▼
🏰 S2 React 城（框架即调度器）────── 05 06 07 08        ← 全书重镇，你的 Hook 洞察在此落地
   │
   ▼
🌉 S3 数据大桥（你的主场）────────── 09 10 11
   │
   ▼
🚚 S4 工程化站（老本行搬家）──────── 12 13 14
   │
   ▼
📱 S5 React Native 港口（换个内核）─ 15 16
   │
   ▼
🏁 S6 毕业设计（全栈合体）────────── 17
```

阅读顺序即编号顺序；每篇 2000~4000 字，配一个可运行实验。总规模约 6~8 万字 + 配套代码。

## 5. 逐站大纲

### S0 装备检查站 —— TypeScript 与异步（3 篇）

**00 · JS/TS 是什么：一门"长在浏览器里的 Java"？**
- 暗号：脚本语言 vs 编译语言 ≈ 解释执行 vs JIT；ESM/CJS 模块 ≈ classpath 与显式 import。
- 边界：JS 没有重载、没有原生多线程、数字只有一种（IEEE 754 双精度，`0.1+0.2 !== 0.3` 的第一现场）。
- 实验：Node 里复现 `0.1 + 0.2`，用 BigDecimal 的心态看 Number 的新欢 BigInt。

**01 · TypeScript：比 Java 更激进的类型系统**
- 暗号：interface/generics/abstract 直接平移；`unknown ≈ Object`，`any ≈ 关掉编译器`，`never ≈ 底部类型`；类型收窄 ≈ 模式匹配/instanceof 分支。
- 核心差异：**结构化类型 vs 名义类型**——Java 看血统（是否 extends），TS 看体检报告（长得像就行）；联合类型 `A | B` 是 Java 里没有的原生代数数据类型；TS 类型是编译期幻象，运行时查无此人（Java 泛型假装擦除，TS 类型真的蒸发）。
- 边界：TS 不做运行时校验，接口校验仍需 zod 这类"运行时类型守卫" ≈ 入参校验层。
- 实验：用联合类型 + 收窄重写一个 Java 里的 `if-else instanceof` 状态机，体会"让非法状态不可表示"。

**02 · 异步之道：事件循环，你早就认识它，它叫 Reactor**
- 暗号：`Promise ≈ Future/CompletableFuture`；`async/await ≈ 更好写的 thenCompose`；事件循环 ≈ Netty 的 EventLoop / NIO Selector——**单线程轮询就绪事件，回调即 handler**。
- 核心差异：JS 的 Future 没有 `get()`，永远不能阻塞等待——主线程是唯一售票窗口，堵了整站瘫痪；微任务队列 ≈ 高优先级就绪队列（本轮清空），宏任务 ≈ 下一轮调度。
- 边界：`CompletableFuture.supplyAsync` 默认扔线程池，`async` 函数不创建任何线程——它是"调度"不是"并发"。
- 实验：手写一个微任务/宏任务交错输出的执行序列题，先猜后跑，猜错即理解。

### S1 运行时站 —— 浏览器即操作系统（2 篇）

**03 · 浏览器即操作系统：进程、线程与"不许卡顿"宪法**
- 暗号：浏览器 ≈ 微内核 OS（多进程隔离：每个标签页一个渲染进程 ≈ 进程隔离防崩溃）；渲染主线程 ≈ 唯一 UI 线程；compositor ≈ 显示服务；Web Worker ≈ 受限的子进程（无共享内存的 worker，通信靠消息传递——Unix 哲学）。
- 边界：你调不了度——没有 `Thread.sleep`，没有线程优先级，只有"把活切成小片排队"。
- 实验：写一个死循环按钮，观察页面冻结，再用 `requestIdleCallback`/分片"治好"它——提前预演 React 的时间切片。

**04 · 闭包、作用域与 GC：匿名内部类的"完全体"**
- 暗号：词法作用域 ≈ 编译期确定的名字查找；闭包 ≈ 捕获外部变量的匿名内部类；GC 可达性 ≈ JVM 可达性分析。
- 核心差异：Java 匿名内部类只能捕获 `final`（拷贝值），JS 闭包捕获**引用**，能持续读写——更自由，也更容易漏。定时器、事件监听器没解绑 ≈ 没 close 的资源句柄，是前端内存泄漏两大惯犯。
- 边界：这里为第 06 站埋钩子——"什么东西能活得比函数栈帧久？"是全书最重要的一个问句。
- 实验：Chrome DevTools Memory 面板抓一次监听器泄漏，用 jmap 的直觉看堆快照。

### S2 React 城 —— 框架即调度器（4 篇，全书重镇）

**05 · 组件与虚拟 DOM：UI = f(state)，渲染 = 一次事务提交**
- 暗号：组件 ≈ 无副作用的纯函数（render）+ 提交（commit）两阶段 ≈ 先算后写的 WAL；虚拟 DOM diff ≈ 脏页标记后最小化回写；**不可变更新 ≈ Copy-on-Write**——`setState` 必须给新对象，因为 React 靠"引用变了没有"判断脏页，直接改旧对象等于偷偷改页缓存还瞒着调度器。
- 边界：VDOM 不是性能银弹，是"可编程的渲染事务"；有些场景裸改 DOM 更快。
- 实验：亲手改 state 内部字段 + 强制刷新 vs 正确创建新对象，观察 React 的"装没看见"。

**06 · Hooks 的内存布局：逃逸的状态与固定槽位**（招牌章，见 3.3 节样章）
- 暗号：Fiber 节点上的 Hook 链表 ≈ 固定槽位的寄存器文件；Rules of Hooks ≈ 调用约定（按序寻址）；`useState` 惰性初始化 ≈ 只在首次 mount 生效 ≈ static 初始化块；`useRef` ≈ 可变实例字段（改它不触发渲染 ≈ 改 transient 字段不触发事件）；`useMemo/useCallback` ≈ 手动缓存，但失效策略自己管，而且**React 可以随时丢弃缓存** ≈ JIT 的乐观优化，是提示不是承诺。
- 边界：state 更新是"调度"不是"赋值"，本批次内读到的是旧值 ≈ 你熟悉的"事务未提交读不到"。
- 实验：实现一个 `usePrevious`，亲眼看到 ref 与 state 的内存位置差异；再用 if 包一个 hook 触发槽位错乱，看 React 报错原文。

**07 · 副作用与生命周期：useEffect 是组件的 @PostConstruct/@PreDestroy**
- 暗号：render 纯计算，commit 后才准摸外部世界 ≈ 事务提交后再发 MQ；effect 清理函数 ≈ finally / @PreDestroy；依赖数组 ≈ 缓存 key——数组就是 key，浅比较就是 equals。
- 边界：React 没有"生命周期方法"的强概念（类组件才有），函数组件只有"每次渲染都是独立快照"——闭包捕获旧值导致的问题（stale closure）≈ 你在 lambda 里捕获过期的局部变量。
- 实验：写一个轮询定时器，先写出泄漏版（依赖写错），再修复，完整体验"清理函数就是 try-finally"。

**08 · 状态管理与通信：共享内存还是消息传递，这是个问题**
- 暗号：props 下行 ≈ 参数传递；事件回调上行 ≈ 监听器模式；Context ≈ IoC 容器的依赖注入（provide/inject）；状态提升 ≈ 集中存储；**Zustand/Redux ≈ 把共享内存访问收敛成"消息驱动的不可变事务"**——不可变更新本质是用 COW 换免锁读。
- 选型立场：原生 hooks + TanStack Query（服务端状态）+ Zustand（客户端状态）为主线；Redux 只讲思想不精讲（≈ 知道 EJB 历史即可）。
- 边界：全局状态 ≈ 全局变量，前端也逃不过"能不共享就不共享"的铁律。
- 实验：同一个购物车，分别用"状态提升"、"Context"、"Zustand"三写，对比代码量与重渲染范围。

### S3 数据大桥 —— 你的主场，浏览器有脾气（3 篇）

**09 · 浏览器里的 HTTP：fetch、CORS 与 Cookie 的存放姿势**
- 暗号：fetch ≈ OkHttp；拦截器（axios interceptor）≈ OkHttp Interceptor / Spring HandlerInterceptor；CORS = 浏览器进程的访问控制（后端视角：那个 OPTIONS 预检是谁发的、为什么我打了日志没看到业务请求）；Cookie/Session/JWT 在前端的存放位置（内存、localStorage、Cookie）与 XSS/CSRF——注入攻击家族的老对手，换个战场。
- 边界：同源策略是浏览器单方面的自律，curl 不受管——这就是"本地能跑线上报错"的头号来源。
- 实验：本地起两个端口互相 fetch，亲手制造并修复一次 CORS 报错。

**10 · TanStack Query：给接口数据装一个 Caffeine**
- 暗号：queryKey ≈ 缓存 key；staleTime/gcTime ≈ TTL + 空闲过期；invalidateQueries ≈ cache evict；乐观更新 ≈ write-behind；重试/退避 ≈ 超时重试策略。这一站你全程舒适，是全书建立信心的制高点。
- 核心认知：**服务端状态不该进 useState**——缓存、去重、失效是系统问题，不该手搓。
- 实验：给第 08 站的购物车接真实接口（配一个 mock 服务），实现失效刷新 + 乐观更新。

**11 · Node.js 侧写：用后端眼光看 BFF（10 分钟版）**
- 暗号：Node ≈ "用 JS 写的 Netty + 内置事件循环"；Express ≈ Spring MVC 极简版（注解变回调）；中间件洋葱模型 ≈ Filter 链/拦截器栈。目的不是转行 Node 后端，而是：看懂前端项目的 mock 服务、SSR、BFF 胶水层。
- 实验：30 行写一个 mock API，供第 10 站实验使用。

### S4 工程化站 —— 老本行搬家（3 篇）

**12 · 包管理与构建：npm ≈ Maven，Vite ≈ javac + 热插拔**
- 暗号：package.json ≈ pom.xml，lock 文件 ≈ 锁版本（依赖地狱不分语言）；语义化版本；Vite dev server ≈ "javac 只编译改动类"+ 热部署；打包摇树 ≈ shade + 死代码消除；source map ≈ 保留行号的混淆反解。
- 实验：把一个项目 build 出来，看产物体积，开/关摇树对比。

**13 · 测试金字塔：Vitest ≈ JUnit 5**
- 暗号：Vitest ≈ JUnit，RTL（测行为不测实现）≈ MockMvc 思想，MSW ≈ WireMock，Playwright ≈ Selenium 的现代款；ESLint/Prettier ≈ Checkstyle + google-java-format；全部挂进 CI。
- 实验：给购物车写一个组件测试 + 一个 E2E 测试，接进 GitHub Actions。

**14 · 部署与可观测：你的 CDN 知识直接平移**
- 暗号：静态资源 + CDN、SPA 路由的 nginx fallback（404 → index.html ≈ 网关路由兜底）、环境变量 .env ≈ application.yml、Docker 多阶段构建（node build → nginx run）≈ 你熟悉的构建产物瘦身、Sentry ≈ 前端 APM。
- 实验：Web 前端打进 Docker 镜像，docker compose 一键连上你的 Spring Boot 后端。

### S5 React Native 港口 —— 同一套 React，换一个内核（2 篇）

**15 · RN 架构：Bridge/JSI ≈ JNI**
- 暗号：JS 线程与原生线程分工 ≈ 进程内多线程；旧架构 Bridge（异步 JSON 消息）→ 新架构 JSI（同步直接调用）≈ 从"走 IPC"升级到"直接函数调用"；原生模块 ≈ JNI 桥接；**Expo ≈ Spring Boot**——约定优于配置，一秒钟建立正确预期。
- 边界：RN 的样式是 CSS 子集 + Flexbox，且没有"浏览器兼容"却有"平台分叉"（Platform.OS ≈ os.name 判断）。
- 实验：Expo 起一个 App，写一个调用原生能力（相机/剪贴板）的页面。

**16 · 从页面到上架：导航、权限与 OTA**
- 内容：react-navigation（路由栈 ≈ 你的网关路由表）、权限模型、OTA 热更新（≈ 不发版的热修复）、双端发布流程概览。
- 实验：给 App 加二级路由 + 一个权限申请。

### S6 毕业设计 —— 全栈合体（1 篇 + 一个仓库）

**17 · Capstone：一个真实小产品的全栈闭环**
- 技术栈：Spring Boot（你写）+ React Web + React Native App + TanStack Query + Docker Compose。
- 候选选题（待拍板，见 §9）：
  - A. 个人监控面板：把自己后端服务的指标/告警做成 Web + App 看板（最贴你的工作）；
  - B. 全栈看板/备忘（Todo 升级版）：经典但完整覆盖 CRUD + 实时 + 离线；
  - C. RSS/信息流聚合 App：有"拉取-缓存-渲染"的完整数据流，最契合"数据读写"主旋律。
- 走完全流程：需求 → 接口设计 → 前端 → 联调（联调一章会集中爆发所有 CORS/环境/代理问题）→ 部署 → 上架。

## 6. 单篇文章的标准模板

```markdown
# 站点编号 · 标题（一个"换名字"式的标题）

## 0. 开场白 —— 一个你经历过的后端场景（痛点共鸣，3 分钟）
## 1. 老朋友 —— 用 OS/Java 的话把原理讲一遍
## 2. 新大陆 —— 前端里它叫什么、长什么样（代码为主）
## 3. 对暗号 —— 映射表（老概念 ↔ 新概念，一张表收束）
## 4. 类比失效的地方 —— 边界、陷阱、"这里千万别套后端直觉"
## 5. 动手实验 —— labs/xx，5~10 分钟跑通 + 预期翻车现场
## 6. 不变量打卡 —— 这一站给"四条不变量表"填了哪一格
## 7. 下一站预告 —— 一句悬念，保持旅程叙事连贯
```

## 7. 语言风格规范

- **人称与口吻**：第二人称"你"，像老同事结对编程时闲聊；娓娓道来 = 先场景后概念、先直觉后定义、先代码后术语。
- **幽默的来源**：来自"准确的自嘲"与两个世界的互相打量（如"React：我用 15 年证明了，你们的线程池，我一个单循环也能跑"），不堆段子、不玩烂梗。
- **段落纪律**：段落 ≤ 5 行；每个抽象概念后 3 行内必须出现具体代码或例子；专有名词首次出现给一句"翻译"。
- **样章基调试写**（第 06 站开场，请审风格）：

> 凌晨一点，你的 Spring 服务这个月第二次因为内存泄漏被重启。你盯着 jmap 输出想：变量总得有地方住吧？恭喜，你已经具备理解 React Hooks 的全部前置知识了。函数组件每次渲染都是从头执行一遍的纯函数，栈帧里的局部变量活不过一次调用——那 useState 凭什么记得住上一次的值？因为它压根没把值放在栈帧里，而是让状态主动"逃逸"到堆上，记进组件在 Fiber 节点上的户口本。你的 JVM 想方设法阻止对象逃逸，React 反其道而行：主动制造逃逸，换取跨帧存活。

## 8. 产物形态与仓库结构（to-full-stack/）

```
to-full-stack/
├── journey-map.svg          # 旅程地图（手绘风路线图，站点可点击）
├── README.md                # 总览：地图 + 阅读顺序 + 进度打卡表
├── 全栈之旅-教程方案-v1.md    # 本文件
├── chapters/
│   ├── 00-js-ts-是什么.md
│   ├── ...
│   └── 17-capstone.md
├── labs/                    # 每站可运行实验（pnpm workspace monorepo）
│   ├── lab-00-number/
│   ├── lab-06-hooks-memory/
│   └── ...
└── site/（可选，后期）        # VitePress/Docusaurus 站点——顺便就是一次 React 实战
```

## 9. 待你拍板的 5 个问题

1. **RN 目标深度**：A. 架构级理解 + 能写基本页面即可；B. 达到能独立上架一个简单 App（含第 16 站完整走一遍）。
2. **状态管理选型**：建议"hooks + TanStack Query + Zustand"为主线、Redux 只讲思想——是否同意？
3. **样章从哪开始**：建议先出 **第 00 站（TS 类型）+ 第 06 站（Hooks 内存布局）** 两篇样章定调，通过后再批量生产。也可以换你更想先读的一站。
4. **毕业设计选题**：§17 的 A/B/C 三选一，或你另指定。
5. **产物形态**：纯 Markdown 仓库即可，还是后期要 VitePress/Docusaurus 站点（本身可当作一次 React 实战教学素材）？

## 10. 生产计划

| 阶段 | 内容 | 交付物 | 验收方式 |
|---|---|---|---|
| P1 样章定调 | journey-map.svg + README + 第 00、06 站全文 + 对应 labs | 2 篇样章可读可跑 | 你审阅文风、深度、映射密度 |
| P2 左半程 | S0→S2 其余 7 篇（重点章 04/05/07/08） | 7 篇 + labs | 按站验收 |
| P3 右半程 | S3→S5 共 8 篇 | 8 篇 + labs | 按站验收 |
| P4 合体 | Capstone 选题与代码骨架 | 完整项目 + 第 17 站 | 一起跑通 |

每篇产出即附可运行实验代码，所有代码在你机器上以 `pnpm` + Node LTS 环境验证跑通后再交付。
