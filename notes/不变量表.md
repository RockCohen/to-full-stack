# 四条不变量 · 全栈统一场对照表

> **前后端都是数据读写**:前端的数据读写用于交互与渲染,后端的用于计算。
> 而背后的**内存管理、进程调度、通信同步(共享/通道/锁)**核心逻辑不变——这就是全书的翻译词典。
>
> 用法:每场学习收尾,把当天新学的前端概念填进右列对应格子;AI 收尾时也会提醒你打卡。
> 填满之日,即融会贯通之时。

| 不变量 | Java 后端世界 | 浏览器前端世界(边学边填) |
|---|---|---|
| **数据放在哪** | JVM 堆、栈帧、元空间、堆外内存 | *待打卡(阶段 1、2)* |
| **谁说了算**(调度) | OS 线程调度、线程池、优先级队列 | *待打卡(阶段 0)* |
| **怎么不打架**(同步/通信) | synchronized、CAS、并发容器、MQ | *待打卡(阶段 2、3)* |
| **数据怎么流动**(I/O) | BIO/NIO/Netty、数据库、多级缓存 | *待打卡(阶段 0、3)* |

## 打卡进度

- [ ] 阶段 0 · S3 后:「谁说了算」格——事件循环、微/宏任务队列
- [ ] 阶段 0 · S4~S5 后:同格补充——回调队列 ≈ 就绪队列,手写过一遍才算数
- [ ] 阶段 0 · S8 后:「数据怎么流动」格——Promise 链、async/await 的 I/O 并发
- [ ] 阶段 1 · 闭包/GC/渲染树 → 「数据放在哪」格
- [ ] 阶段 2 · Fiber 槽位、不可变更新、Context → 前两行
- [ ] 阶段 3 · TanStack Query ≈ Caffeine → 「数据怎么流动」格
- [ ] 阶段 5 · 总答辩《全栈统一场 10 问》——考的就是这张表
