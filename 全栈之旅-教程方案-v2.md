# 《全栈之旅》教程方案 v2

> 副标题：**写给 Java 后端工程师的前端通关手册**
> 形式：**完全对齐 ai4me/llm-journey 的真实形态**——阶段制 + AI 助学手册（场次制）+ prompt 卡 + 禁写区 + 答辩验收 + 坏代码门诊 + 误解记录本。
> 本方案取代 v1（v1 把它误判成了"文章式教程"，形态判断错误，方法论部分保留并入 v2）。

---

## 1. 对 llm-journey 的形态诊断（v2 的设计依据）

读了 `~/Desktop/ai4me/llm-journey` 的 README、阶段 README、AI助学手册和 prompt 卡，它的本质**不是教程文章集，而是一套"你 + AI 助教"的操作手册**。可迁移的机制清单：

| llm-journey 的机制 | 具体做法 | 本方案是否采用 |
|---|---|---|
| 阶段制（phase0~5） | 每阶段一个目录：README（材料清单/任务清单/验收标准/常见卡点/毕业去向） | ✅ 同样六阶段 |
| 场次制（S1~S9） | 每场 2~2.5h，场次总表列"时长/完成标志/产出落点" | ✅ 全盘照搬 |
| ⓪ 号契约卡 | 每次新会话先贴：禁写区、摸底、仲裁、误解记录、收尾 | ✅ 出前端版 |
| prompt 卡体系 | ①摸底 ②预测实验 ③禁写区开工 ④卡壳求助 ⑤答辩考官 ⑥门诊线索 ⑦复习题 | ✅ 出前端版 |
| 预测式实验 | 先写预测（形状/数值/报错？）再运行，错题出变体直到连对 | ✅ 前端版预测"输出/报错/渲染几帧" |
| 禁写区 | AI 不写核心代码，三级引导（L1 概念提问→L2 指向性提问→L3 一行伪代码） | ✅ 见 §4，本书最重的设计 |
| 答辩十问 | defense_XXX_10问.md，通过线 ≥8，答错进误解记录本 | ✅ 每阶段一套 |
| 坏代码门诊 | 5 个可运行脚本各藏 1 个 bug，先诊断假设再验证，答案只许复核 | ✅ 前端 bug 全是"后端直觉翻车现场"，天然适配 |
| 误解记录本 | misconceptions.md，阶段末成为最个人化的复习材料 | ✅ 原样 |
| AI 考官制验收 | 1 小时脱稿构建 + 3 道现场附加题 ≥2 道 | ✅ 原样 |
| 毕业 tag | `git tag phase0-graduate` + 毕业去向 | ✅ 原样 |
| 提交习惯 | 每个小实验一 commit + 三五行情式笔记；节奏慢但不能断 | ✅ 原样 |
| Web 互动课程 | 阅读视图 + 模拟器视图，每场开工前 10 分钟预演 | ⚠️ 列为后期选项（§7） |
| 核心哲学 | "视频教材降级为字典，先建构后字典；**AI 能替你写代码，但替不了你长出理解**" | ✅ 全盘继承 |

**不迁移的**：llm-journey 的外部材料主线（Karpathy 视频、d2l 教材）。前端没有等价的"唯一权威课"，替代方案是：官方文档（react.dev / typescript-handbook）当字典 + 本仓库的"映射讲义"当主线讲义（这是全书唯一"读"的部分，见 §5）。

## 2. v1 保留的资产：映射方法论（内容的灵魂）

形式学 llm-journey，**教学内容的核心仍是你的洞察**，三点不变：

1. **四条不变量主旋律**：前后端都是数据读写（前端用于交互渲染、后端用于计算）；内存管理、进程调度、通信同步（共享/通道/锁）的逻辑不变。每场收尾往 `notes/不变量表.md` 打卡一格，全书读完表即成。

   | 不变量 | Java 后端世界 | 浏览器前端世界 | 主战场 |
   |---|---|---|---|
   | 数据放在哪 | JVM 堆/栈帧/元空间 | JS 堆（闭包、Fiber 槽位）、渲染树、Storage | 阶段 1、2 |
   | 谁说了算 | OS 线程调度、线程池 | 事件循环单线程 + 微/宏任务队列 + 浏览器多进程 | 阶段 0、1 |
   | 怎么不打架 | synchronized/CAS/MQ | 不可变更新（COW）、单向数据流、订阅 | 阶段 2、3 |
   | 数据怎么流动 | NIO/Netty、多级缓存 | fetch、WebSocket/SSE、TanStack Query | 阶段 3、4 |

2. **对暗号**：每个新概念先猜"它在 Java/OS 里像什么"，猜完再对答案——这恰好和 llm-journey 的"生成先于接收"完美咬合，直接写进 ⓪ 号契约卡第 0 条。
3. **类比失效的地方**：每场实验卡和答辩题必考"哪里不能套后端直觉"。

招牌章（v1 第 06 站）升级为**阶段 2 的禁写区**：手写 mini-React hooks，亲手造出"状态从栈帧逃逸到 Fiber 堆上的固定槽位"——这就是本仓库的 micrograd 时刻。

## 3. 六阶段总览与场次总表

总规模：**6 阶段 ≈ 43 场 ≈ 100h ≈ 3 个月**（每周 8~10h，节奏可慢不可断）。

| 阶段 | 主题 | 场次 | 时长 | 禁写区产出 | 毕业 tag |
|---|---|---|---|---|---|
| phase0 | TypeScript 与异步模型 | S1~S8（8 场 ≈ 20h） | 2 周 | 手写 Promise（简版 A+） | `phase0-graduate` |
| phase1 | JS 运行时与内存（浏览器即 OS） | S9~S15（7 场 ≈ 17h） | 2 周 | 手写响应式 store（订阅+不可变更新） | `phase1-graduate` |
| phase2 | React：框架即调度器 ⭐全书重镇 | S16~S24（9 场 ≈ 22h） | 3 周 | 手写 mini-React + hooks 槽位链表 | `phase2-graduate` |
| phase3 | 数据与网络（你的主场） | S25~S30（6 场 ≈ 14h） | 1.5 周 | 无（配置型知识，改为变异实验） | `phase3-graduate` |
| phase4 | 工程化与部署（老本行搬家） | S31~S35（5 场 ≈ 12h） | 1.5 周 | 无；脱稿验收 = 1h 搭出完整流水线 | `phase4-graduate` |
| phase5 | React Native + 全栈毕业设计 | S36~S43（8 场 ≈ 20h） | 2~3 周 | Capstone 全栈项目 + 总答辩 | `fullstack-graduate` |

### 各阶段任务清单（硬指标摘要）

**phase0 · TS 与异步**：类型系统速通（结构化类型/联合类型/收窄，对照 Java 名义类型）；事件循环与 Promise（≈CompletableFuture + Reactor EventLoop，但没有阻塞 `get()`）；**禁写区手写 Promise**（状态机 + 回调队列 + then 链）；答辩《事件循环 10 问》。

**phase1 · 运行时与内存**：浏览器多进程架构（≈微内核 OS）；闭包与 GC（≈匿名内部类的完全体 + 可达性分析）；内存泄漏门诊（监听器/定时器 ≈ 忘 close 的句柄）；**禁写区手写 20 行响应式 store**——用"共享内存→消息订阅"给 React 铺路；答辩《闭包与内存 10 问》。

**phase2 · React**：组件与 VDOM（UI=f(state)，不可变更新 ≈ COW，diff ≈ 脏页最小回写）；hooks 内存模型（useState 槽位链表、useRef 实例字段、useEffect ≈ @PostConstruct/@PreDestroy + 依赖数组≈缓存 key）；状态管理与通信（Context≈IoC 注入，Zustand≈消息驱动的不可变事务）；**禁写区手写 mini-React**（createElement + render + 简易 diff + useState/useEffect）；答辩《Hooks 内存模型 10 问》《状态与渲染 10 问》。

**phase3 · 数据与网络**：fetch/CORS/Cookie-JWT 存放姿势与 XSS/CSRF；TanStack Query（queryKey≈缓存 key、staleTime≈TTL、invalidate≈evict、乐观更新≈write-behind）；Node 十分钟（Express≈Spring MVC 极简版，为 mock 服务）；变异实验三组（staleTime/batch/重试策略，每组先预测再运行）；答辩《CORS 与缓存 10 问》。

**phase4 · 工程化**：npm≈Maven、Vite≈javac+热插拔、摇树≈死代码消除；Vitest≈JUnit、RTL≈MockMvc 思想、Playwright≈Selenium、MSW≈WireMock；静态托管+CDN、nginx fallback、Docker 多阶段构建、Sentry≈APM；**脱稿验收：1 小时从空目录搭出"构建+测试+Docker 镜像"流水线** + AI 考官 3 道附加题。

**phase5 · RN 与毕业**：RN 架构（JSI≈JNI，Expo≈Spring Boot）；导航/权限/OTA；**Capstone**（选题见 §8）：Spring Boot + React Web + RN App + Docker Compose 一键起；全流程 联调→部署→总答辩《全栈统一场 10 问》（考的就是不变量表）。

## 4. 禁写区设计（本仓库的三个 micrograd 时刻）

| 禁写区 | 对应 llm-journey | 为什么是它 |
|---|---|---|
| **手写 Promise**（phase0，≈100 行） | micrograd Value 类 | Promise 之于异步，就是 autograd 之于训练循环。状态机 + 回调队列 + then 链，写完 Promise.all/race 顺手就懂。对 Java 人 ≈ 手写 CompletableFuture 的调度内核 |
| **手写响应式 store**（phase1，≈20 行） | micrograd Ⅱ | 观察者 + 不可变更新，是"锁机制 → 消息机制"映射的实做，也是 React 状态库的共同祖先 |
| **手写 mini-React + hooks**（phase2，≈150~200 行） | micrograd 全套 | createElement→vdom→diff→commit→useState 槽位链表→useEffect 提交后回调。亲手造完，"hook 是内存管理"就不用再讲了——你自己写的 |

禁写区纪律与 llm-journey 完全一致：AI 不输出核心实现代码，三级引导，脚手架归 AI、核心归你（如 mini-React 的 JSX 转换函数由 AI 提供，diff 与 hooks 必须自己写）；完成标志是**对拍全绿**（Promise：用 promises-aplus-tests 简版用例；mini-React：与官方 React 行为对照实验全过）。

## 5. "映射讲义"——全书唯一以读为主的部分

llm-journey 用视频当字典，前端改为每阶段一篇 **MAP.md（映射讲义，2000~4000 字）**：开场痛点场景 → 老朋友原理 → 新大陆对照 → 映射表 → 类比失效清单。文风按 v1 §7 规范（娓娓道来 + 风趣幽默，段落 ≤5 行，概念后 3 行内必有代码）。

**写作铁律（2026-09 随 web 样板确立，全课程强制）：**

1. **映射先行**：每个概念必须"旧世界先行"——先亮 Java/CS 里已有的代码或场景，再给新世界的孪生，再做综合分析；禁止连续两段纯前端叙述而不回锚。
2. **读写世界观**：每章开头用 worldview 框声明"本章管的是哪一类读写"，正文用读写语言点题，并找到本章的"审计/调度/仲裁"主线索（如 c00 的"读写审计不断左移"、c01 的"事件循环＝存储引擎"）；结尾打卡不变量表。
3. **零 Web 基础（有校准门槛）**：读者没有任何 TS/JS/HTML/CSS 基础。这四样的概念（含 DOM、CSS、选择器）首次出现，要么当段解释、要么就地进"🧰 发散补充盒"（可折叠，老手跳过、新手必读），并锚一个 Java/CS 对应物；主讲线上只允许出现已解释过的概念。**发散盒只补"无法从名字或形状一眼认出"的概念——console.log、setTimeout 这类一眼可辨的不提，宁缺毋滥。**
4. **视角恒等式**：读者永远是"带着后端经验来的人"——对话、类比、提问都从读者嘴里发出、向读者的经验回归；禁止把读者摆进"被科普的前端"角色。
5. **业务宇宙统一**：全课程例题逐步收敛到同一条虚构业务线——**"订单与支付"**（天然覆盖状态机、Result、并发竞态、限流、缓存失效、推送），让读者的心智模型累积而非每章重置；新内容一律取材于它，存量例题在各自修订窗口迁移，不批量翻新已审阅内容。

用法与 llm-journey 一致：**讲义也是字典**——先做场次实验暴露概念缺口，卡壳才回读对应小节；读在"建构"之后，不在之前。

## 6. tutor/ 材料设计（每阶段标准配置）

```
tutor/
├── prompts.md                  # ⓪契约卡（前端版）+ ①~⑦操作卡
├── experiments_sN_<主题>.md     # 预测式实验卡（每场一份）
├── defense_<主题>_10问.md       # 答辩题库
├── bug_clinic/                 # 5 个藏 bug 可运行脚本 + ANSWERS.md
└── misconceptions.md           # 误解记录本
```

**⓪ 号契约卡前端版**（在 llm-journey 五条之上新增第 0 条，其余保留原文精神）：

```text
你是我"Java 后端转前端"阶段的 AI 助教，全程遵守以下约定：

0.【映射教学法】每引入一个新概念，先让我猜"这在 Java/OS 里像什么"，我猜完
   再对答案；讲解必须给出"老概念 → 新概念"映射表，并明确指出映射在哪里
   失效（此处最容易埋误解）。
1.【禁写区】凡标注"禁写区"的练习（本仓库：手写 Promise、响应式 store、
   mini-React），你不准输出核心实现代码。引导只有三级：L1 概念提问 →
   L2 指向性提问 → L3 一行伪代码；我不主动要提示，你就停在前一级。
2.【生成先于接收】新概念先出 2~3 道摸底题，等我答完再讲；只讲我答错的部分，
   并默认我有 Java 后端背景，跳过线程、HTTP、缓存等我已会的铺垫。
3.【代码是仲裁者】概念分歧写 5~10 行代码跑一跑，谁输听谁的。
4.【误解记录】发现我的理解偏差时，末尾追加：
   误解记录｜日期｜我的误解｜纠正｜一道复习题
5.【收尾三件套】检查 commit 了吗、笔记写了吗；出 3 道"一周后的我"复习题；
   并提醒我把本场新概念填进 notes/不变量表.md 的对应行。
```

**门诊选题示例**（全部对应"后端直觉翻车现场"）：then 链里忘了 return（≈吞掉 Future 结果）；useEffect 依赖数组写错导致死循环请求（≈缓存击穿风暴）；直接改 state 不触发渲染（≈偷改页缓存瞒着调度器）；闭包持有监听器不清理（≈没 close 的连接）；列表 key 用 index 导致状态串位（≈用可变字段当数据库主键）；CORS 预检没配（≈网关没放行 OPTIONS）。

## 7. 仓库结构（to-full-stack/）

```
to-full-stack/
├── README.md                    # 路径总纲：六阶段路线图、进度表、提交习惯
├── 全栈之旅-教程方案-v2.md        # 本方案
├── package.json + pnpm-workspace.yaml
├── scripts/verify_env.mjs       # 环境自检（node/pnpm/浏览器/React 可跑）
├── notes/
│   ├── 不变量量表.md              # 四条不变量主表，每场打卡一格 ⭐
│   └── 杂记.md
├── web/                     # 🌐 互动课程平台(已上线样板:c00 类型收窄 / c01 事件循环)
│                                # 阅读视图 + 模拟器视图,形式对齐 llm-journey 的 web/
├── phase0-ts-async/
│   ├── AI助学手册.md             # 场次表 S1~S8 + 每场操作单
│   ├── README.md                # 材料清单、任务清单、验收标准、常见卡点、毕业去向
│   ├── MAP.md                   # 映射讲义（TS≈失散兄弟 / 事件循环≈Reactor）
│   ├── tutor/                   # prompts / experiments / defense / bug_clinic
│   ├── exercises/               # 01_类型收窄 02_异步时序 03_禁写区手写Promise 04_脱稿验收
│   └── notes.md
├── phase1-runtime/ …            # 同构：S9~S15
├── phase2-react/ …              # 同构：S16~S24 ⭐禁写区 mini-React
├── phase3-data-net/ …           # S25~S30
├── phase4-engineering/ …        # S31~S35
└── phase5-rn-capstone/ …        # S36~S43 + capstone/ 项目仓库
```

## 8. 待你拍板的问题（v1 的 5 问修订为 4 问）

1. **禁写区起点**：phase0 禁写区按计划是"手写 Promise"，OK？它替代了 v1 的"样章"——你现在就可以从 S1 开学，phase0 全套材料就是第一个交付物，先用一周既是验收产品又是开学。
2. **状态管理选型**：hooks + TanStack Query + Zustand 主线、Redux 只讲思想——是否同意？
3. **Capstone 选题**：A. 个人监控面板（贴工作）；B. 全栈看板/备忘；C. RSS 聚合 App（最贴"数据读写"主旋律）。
4. **Web 互动平台**：llm-journey 有 web/（阅读视图+模拟器视图）。建议**先跑手册版，平台列为后期**（模拟器：事件循环动画、hooks 槽位链表、VDOM diff——本身也是很好的 React 实战）。要不要纳入计划？

## 9. 生产计划

| 阶段 | 交付物 | 验收方式 |
|---|---|---|
| P1 | 仓库骨架 + verify_env + ⓪契约卡 + **phase0 全套**（README/手册/MAP.md/tutor 全部/练习骨架） | 你直接开学 S1，边学边验收 |
| P2 | phase1、phase2 全套（含 mini-React 禁写区脚手架与对拍脚本） | 每阶段学完你验收 |
| P3 | phase3、phase4 全套 | 同上 |
| P4 | phase5 + capstone 骨架代码 + 总答辩题库 | 跑通毕业 |

所有练习代码在你机器上以 Node LTS + pnpm 验证可跑后再交付；预测实验卡每题保证"5~15 行内可复现"。

> **质量与连续性门槛**：① 每套新材料发布前过一遍"红队 pass"——换一个只挑错的 AI 视角，复核题面、预测答案与代码；② phase1 材料必须在读者 phase0 毕业（S8）之前就绪，杜绝旅程断头路；③ web 章随场次同步补齐（S2/c02、S4~S5/c03 优先级最高）。
