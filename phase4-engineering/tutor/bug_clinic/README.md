# 坏代码门诊(阶段 4 · 工程化事故现场)

> 5 个病例:1~3 号可运行(`pnpm lab`),4~5 号是**读档诊断**(bug 藏在配置里,读档→诊断→最小修复)。
> 病根全是"后端工程直觉的错位":锁文件、副作用、测试隔离、SPA fallback、构建缓存。

## 流程(每例)

1. 运行或读档观察症状
2. **写下诊断假设** → 改最小的一处验证 → 修复
3. 对 [ANSWERS.md](./ANSWERS.md) 复核;每例产三行笔记:**症状 / 根因 / 后端对照**

## 病历列表

| 门诊号 | 文件 | 一句话症状 | 形式 |
|---|---|---|---|
| 1 | `bug1_lockfile_drift.ts` | 同一份 package.json,CI 装出的版本和同事电脑不一样 | 可运行 |
| 2 | `bug2_side_effect_bundle.ts` | 只用了一个函数,产物里却躺着整个模块——还包括它的副作用 | 可运行 |
| 3 | `bug3_test_pollution.ts` | 两个测试单独跑全绿,一起跑就红 | 可运行 |
| 4 | `bug4_spa_fallback_404.md` | 首页好好的,用户刷新 /orders 直接 404 | 读档诊断 |
| 5 | `bug5_dockerfile_cache.md` | 改一行代码,Docker 把全部依赖重装一遍;镜像里还藏着 node_modules | 读档诊断 |

## 规则

- ANSWERS.md 只许复核用;先诊断假设,再验证;
- 每例三行笔记:症状 / 根因 / 后端对照;
- 全部治完 = 本阶段毕业的前置条件。
