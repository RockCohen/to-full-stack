# 答辩 · 工程化流水线 10 问

> 用法:整个文件发给 AI,逐题作答,它逐题追问。通过线 **≥8**。

1. semver 的 `^` 与 `~` 各自的漂移半径?"不是所有人都守约定"时,你的哪件武器兜底?
2. 锁文件的哲学:它记录的是"快照"还是"誓言"?应用与库的锁死策略为什么不对称?
3. 幽灵依赖是什么?pnpm 的符号链接如何在文件系统层面消灭它?类比类加载器可见性讲一遍。
4. Vite dev 快与 prod 快分别来自什么?为什么 dev 形态不能直接上线?
5. 摇树为什么不敢删"有副作用的模块"?`sideEffects: false` 是谁向谁的担保?担保错误的代价?
6. 静态资源"入口 no-cache + 哈希产物一年缓存"的协议,为什么两者缺一不可?发布流程里谁负责让协议闭环?
7. `npm ci` 与 `npm install` 的差别?流水线里哪一步用哪个,错了会怎样?
8. 测试替身(mock/stub)的本质是什么?MSW 与模块级 mock 的分层差别,对应 WireMock 与 Mockito 的哪个差别?
9. Docker 多阶段构建"少装就是赚":runtime 镜像里不该有什么?层缓存的"先拷什么后拷什么"原则及理由?
10. **综合题**:给你 1 小时和一台空机器,从零搭"构建+测试+Docker 镜像"流水线——写出你的步骤清单与每步的失败回滚思路(这就是 S35 的考纲)。

---

*通过后:错题进误解本 → 修补 → AI 出变体题复测。S35 考官会抽 3 道口试。*
