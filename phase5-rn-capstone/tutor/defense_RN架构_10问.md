# 答辩 · RN 架构 10 问

> 用法:整个文件发给 AI,逐题作答,它逐题追问。通过线 **≥8**。

1. RN 为什么必须有一条"通道"?浏览器里的 JS 调 DOM API 为什么不需要?
2. 旧 Bridge 与 JSI 的本质差别(异步/同步、序列化/直引用)?为什么说新架构是"复杂度归位"而不是单纯"更快"?
3. 哪些值过不了旧 Bridge 的安检?为什么函数天然过不去?JSI 怎么等价解决?
4. RN 的三条线程各管什么?JS 线程跑死循环,UI 会怎样、什么交互会失灵?
5. View/Text/StyleSheet 与 div/span/CSS 的对应与缺失(级联/继承)?哪个缺失最反直觉?
6. FlatList 与 map 的本质差别?窗口化回收 ≈ 你后端的哪两件事?keyExtractor 为什么必须业务 id?
7. Web 的"离开页面"与 RN 的"离开屏幕"在生命周期上的本质差别?由此推出的清理纪律是什么?
8. 运行时权限的三态(授予/拒绝/永久拒绝)各该配什么 UI?为什么说拒绝态是正常路径?
9. OTA 更新能更新什么、不能更新什么?它的回滚与灰度怎么设计?与配置中心热下发像在哪?
10. **综合题**:给团队写一份"RN 接入现有 Spring Boot API"的接入规范,至少覆盖:BaseURL 环境区分、Query 纪律(invalidate 同构)、权限处理、发布兼容(先加后删)——每条附机制层理由。

---

*通过后:错题进误解本 → 修补 → AI 出变体题复测。S43 总答辩会抽 RN 相关口试。*
