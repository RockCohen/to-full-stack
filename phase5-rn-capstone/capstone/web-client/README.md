# capstone-web-client

M2 验收:

```bash
cd capstone/web-client
npm install
npm run dev          # http://localhost:5173(需 server 已启动)
```

- 数据层:`src/api.ts`(failFast + 超时 TODO 归你)
- 看板:`src/App.tsx`(queryKey / mutation 失效 TODO 归你)
- 生产构建:`npm run build`(M4 由 compose 托管)
