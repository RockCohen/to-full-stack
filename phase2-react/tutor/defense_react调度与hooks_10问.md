# 答辩 · React 调度与 Hooks 10 问

> 用法:整个文件发给 AI,逐题作答,它逐题追问。通过线 **≥8**。

1. "UI = f(state)"——这个纯函数签名里,f 的输入是什么、输出是什么、"写"发生在哪一步?
2. 渲染函数为什么必须是纯函数?StrictMode 故意跑两遍在防什么?
3. props 和 state 的本质区别?什么信号告诉 React"该重渲染了"?
4. 状态提升与 Context 各解决什么问题?滥用 Context 的代价是什么?
5. useEffect 的依赖数组为什么是"缓存 key"?漏写一个依赖会发生什么?用"读到旧缓存"的语言讲。
6. 清理函数的执行时机有哪两种?"启动 A → 清理 A → 启动 B"为什么是这个顺序而不是"启动 B → 清理 A"?
7. 轮询、订阅-通知、重渲染＋diff,三种"状态变化感知"方案各自的成本?React 选第三条路的代价与收益?
8. stale closure 是怎么产生的?它和你后端"lambda 捕获过期局部变量"的差别在哪?
9. 无限重渲染循环(渲染函数里调 setState)为什么会发生?React 怎么发现它?
10. **综合题**:给团队新人写一条 useEffect 使用规范,至少包含三条"必须"与两条"禁止",每条附机制层理由。

---

*通过后:错题进误解本 → 修补 → AI 出变体题复测。S24 考官会抽 3 道口试。*
